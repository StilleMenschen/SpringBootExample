<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tech.tystnad.works.repository.mapper.SysUserVOMapper">
    <select id="findByDTO" parameterType="tech.tystnad.works.model.dto.SysUserDTO" resultType="tech.tystnad.works.model.vo.SysUserVO">
        SELECT u.user_id, u.org_id, o.org_name, u.top_id, u.user_name, u.role_id, u.nickname, u.email, u.telephone_number, u.user_type, u.enabled, u.create_time, u.updater, u.creator, cu.user_name AS creator_name, uu.user_name AS updater_name
        FROM sys_user AS u JOIN sys_organization AS o ON (u.org_id = o.org_id) JOIN sys_user AS uu ON (u.updater = uu.user_id) JOIN sys_user AS cu ON (cu.user_id = u.creator)
        WHERE u.deleted = 0
        <if test="userId != null">
            AND u.user_id = #{userId}
        </if>
        <if test="orgId != null">
            AND u.org_id = #{orgId}
        </if>
        <if test="orgName != null">
            AND o.org_name LIKE CONCAT(#{orgName},'%')
        </if>
        <if test="topId != null">
            AND u.top_id = #{topId}
        </if>
        <if test="userName != null">
            AND u.user_name LIKE CONCAT(#{userName},'%')
        </if>
        <if test="roleId != null">
            AND u.role_id = #{roleId}
        </if>
        <if test="nickname != null">
            AND u.nickname LIKE CONCAT(#{nickname},'%')
        </if>
        <if test="email != null">
            AND u.email = #{email}
        </if>
        <if test="telephoneNumber != null">
            AND u.telephone_number LIKE CONCAT(#{telephoneNumber},'%')
        </if>
        <if test="userType != null">
            AND u.user_type = #{userType}
        </if>
        <if test="enabled != null">
            AND u.enabled = #{enabled}
        </if>
        <if test="createTimeStart != null">
            AND u.create_time <![CDATA[>=]]> #{createTimeStart}
        </if>
        <if test="createTimeEnd != null">
            AND u.create_time <![CDATA[<=]]> #{createTimeEnd}
        </if>
        <if test="creatorName != null">
            AND cu.user_name LIKE CONCAT(#{creatorName},'%')
        </if>
        <if test="updaterName != null">
            AND uu.user_name LIKE CONCAT(#{updaterName},'%')
        </if>
    </select>
</mapper>