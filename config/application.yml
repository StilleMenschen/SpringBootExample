# Server configuration
server:
  port: 8080
#  contextPath:

# Spring configuration
spring:
  jackson:
    serialization:
      INDENT_OUTPUT: true
  data.mongodb:
    host: localhost
    port: 27017
    database: test

# JWT
jwt:
  header: Authorization
  secret: ow9ycVTT
  expiration: 604800
  tokenHead: "Bearer "
  route:
    authentication:
      login: auth
      refresh: refresh
      register: "auth/register"

# Logging configuration
logging:
  level:
    tech.tystnad.joblog: DEBUG
    org.springframework:
      data: WARN
      security: WARN

# Swagger Enable (true,false)
swagger:
  enable: true
#spring.boot.admin.url: http://localhost:8080
